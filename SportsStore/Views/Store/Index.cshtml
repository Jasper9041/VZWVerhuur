@model PagedList.IPagedList<SportsStore.Models.Domain.Product>
@using PagedList.Mvc;

@{
    ViewBag.Title = "VZW De Kangoeroes: Catalogus";
    ViewBag.Active = "Store";
}

<h2>Catalogus</h2>
@*@using (Html.BeginForm("Index", "Store"))
{
    <div class="row col-md-12">
        <div class="form-inline">
            <div class="form-group">
                @Html.Label("CategoryId", "Categorie")
                @Html.DropDownList("CategoryId", ViewBag.Categories as SelectList, "Alle categorieën", new {@class = "form-control"})
            </div>
            <button type="submit" class="btn btn-default">Filteren</button>

        </div>
       
    </div>


<div class="row">
    <div class="col-md-12">
    <p class="info">Alle prijzen zijn richtprijzen.</p></div>
</div>

}*@
<div class="btn-group categorieBtn" role="group" aria-label="">
   
        @Html.ActionLink("Alle categorieën", "Index", 0, new { @class = "btn btn-default " })
        @foreach (var category in ViewBag.Alle)
        {
            @*@Html.ActionLink((string)category.Name, "Index", new { categoryId = category.CategoryId }, new { @class = "btn btn-default" })*@
            <input type="button" name="categoryId" class=" btn btn-default categorieBtn" value="@category.Name"/>
            @*@Html.*@

        }
    
   
</div>


<hr class="col-md-12" />
<div id="producten">
    @Html.Partial("_StorePartial",Model)
</div>
Pagina @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) van @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("Index",
    new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))





